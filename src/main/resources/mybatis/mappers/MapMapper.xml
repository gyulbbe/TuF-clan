<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.gyulbbe.map.mapper.MapMapper">

    <!-- 맵 등록 -->
    <insert id="insertMap" parameterType="insertMapDto">
        INSERT INTO map (
            match_info_id,
            user_id
        ) VALUES (
            #{matchInfoId},
            #{userId}
        )
    </insert>

</mapper>
